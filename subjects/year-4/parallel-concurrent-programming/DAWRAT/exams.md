# بنك أسئلة اختيار من متعدد - برمجة تفرعية (مستخرجة من دورات سابقة)

## الجزء الثالث: أسئلة اختيار من متعدد (MCQ)

---

# ==========================================================
# الدورة 1: امتحان برمجة تفرعية - الفصل الثاني 2023/2024 (النموذج B)
# ==========================================================

**المصدر:** [نمط 2023/2024 — الفصل الثاني]
### السؤال 1 (سهل)
لا يُمكن استخدام كائنات `Future` في تحقيق حلول تفرعية في حالة البرمجة التابعة Functional.
أ) صح
ب) خطأ
**الإجابة الصحيحة: ب**
**التعليل:**
العبارة خاطئة. `Future` هو من أهم أدوات `Functional Parallelism` وليس عائقاً له. في المحاضرة 3 (Functional Parallelism)، تُستخدم `Futures` كطريقة أساسية لتنفيذ عمليات متوازية مع الحصول على القيم الراجعة. `Future` هو حاوية تُملأ بنتيجة مهمة قد تشتغل بالتوازي، وأي خيط تاني يقدر يسحب نتيجتها بـ `get()` — وهذا بالضبط يدعم برمجة تفرعية وظيفية.

الخلط الشائع: قد يخلط الطالب بين `Lazy Memo` و `Future` — لكن كلاهما يدعم `Functional Parallelism`. الفرق: `Lazy Memo` تُحسب بنفس الخيط عند الطلب، أما `Future` ممكن تُحسب بخيط منفصل حقاً.

#### تذكرة:
> من المحاضرة 3 §3.2: `Future` هي حاوية بتُملأ بنتيجة مهمة قد تشتغل بالتوازي. أي خيط ممكن يسحب النتيجة بـ `get()` — وهذا بالضبط اللي بيخليها أداة موازية قوية. بالبرمجة الوظيفية، الفكرة إنك ما بتعدّل حالة مشتركة، بس بتاخد مدخلات وترجع مخرجات — و `Future` بتدعم هالمبدأ بس مع السماح للعملية تصير بالتوازي.

**المصدر:** [نمط 2023/2024 — الفصل الثاني]
### السؤال 2 (سهل)
التعليمات `next` المستخدمة لتحقيق التزامن في مفهوم الحاجز Barrier تتضمن عمليتين هما: `SIG+WAIT`.
أ) صح
ب) خطأ
**الإجابة الصحيحة: أ**
**التعليل:**
العبارة صحيحة. في المحاضرة 4 (Loop Parallelism)، عند استخدام `forallPhased` و `next()`، الحاجز يعمل بمبدأ Split-Phase: تُقسم عملية الانتظار إلى مرحلتين. `SIG` (Signal/arrive): الخيط بيسجل وصوله ويكمل فوراً. `WAIT` (awaitAdvance): بعدين يقف وينتظر كل الخيوط توصل قبل ما تكمل. هاد بالضبط ما تشرحه المحاضرة 5 عن `Phasers` — بمكننا تقسيم الحاجز لـ `arrive()` (SIG) و `awaitAdvance(phase)` (WAIT)، وبينهم نحط شغل محلي مستقل.

#### تذكرة:
> من المحاضرة 5 §1.2: `ph.arrive()` = SIG (تسجيل الوصول فقط بدون انتظار)، `ph.awaitAdvance(phase)` = WAIT (انتظر الآن لحد كل الأطراف توصل). الفرق عن `arriveAndAwaitAdvance()` إنها تجمع العمليتين، بينما التقسيم بيسمح بشغل مستقل في النص.

**المصدر:** [نمط 2023/2024 — الفصل الثاني]
### السؤال 3 (سهل)
يُمكن تحقيق مفهوم الحلقات التكرارية التفرعية باستخدام مفهوم Streams.
أ) صح
ب) خطأ
**الإجابة الصحيحة: أ**
**التعليل:**
العبارة صحيحة. في المحاضرة 3 (Functional Parallelism)، يُشرح أن `Java Streams` يمكنها تنفيذ العمليات التكرارية بشكل متوازٍ عبر `.parallel()`. `Streams` بتعميم فكرة الكسول (Laziness) على مجموعات كاملة من العناصر، ودعم التنفيذ المتوازي. عمليات مثل `map()`, `filter()` هي عمليات وسيطة (intermediate) بتشتغل lazy، وأي عملية نهائية (terminal) مثل `collect()` بتجبر التنفيذ الفعلي. بـ `.parallel()` هاد التنفيذ بيصير موازياً على العناصر المختلفة.

#### تذكرة:
> من المحاضرة 3 §3.3: `Java Streams` بتعميم فكرة `Lazy Evaluation` على مجموعات كاملة. العمليات الوسيطة ما بتنفذ شيء لحد ما توصل عملية نهائية. بـ `.parallel()` التنفيذ بيصير موازياً — كل عنصر بيعالج بخيط منفصل أو مهمة منفصلة (حسب حجم مجموعة البيانات).

**المصدر:** [نمط 2023/2024 — الفصل الثاني]
### السؤال 4 (سهل)
من أجل كائن Phaser وحيد: عند عمليات `awaitAdvance` يساوي عدد عمليات `arrive` دائماً.
أ) صح
ب) خطأ
**الإجابة الصحيحة: أ**
**التعليل:**
العبارة صحيحة. في المحاضرة 5 (Data Flow Synchronization)، عند استخدام `Phaser` بشكل صحيح، كل طرف (party) بيستدعي `arrive()` ثم `awaitAdvance()` في كل مرحلة. نقطة الرئيسية: لو Phaser مهيى بـ `n` أطراف (parties)، بكل مرحلة:
- عدد استدعاءات `arrive()` بيساوي `n` (كل طرف بيسجل وصول مرة)
- كل واحد من الأطراف بيستدعي `awaitAdvance()` ويستنى لحد كل الـ `n` أطراف توصل

فعملياً عدد استدعاءات `awaitAdvance()` = عدد استدعاءات `arrive()` = عدد الأطراف × عدد المراحل. لكن القصد بالسؤال الأرجح: بنفس المرحلة، عدد ما يستنى (awaitAdvance) = عدد ما يصل (arrive).

#### تذكرة:
> من المحاضرة 5 §1.2: `Phaser ph = new Phaser(n)` — تهيئ الـ Phaser بـ `n` أطراف. كل واحد بيستدعي `arrive()` (تسجيل وصول) ثم `awaitAdvance(phase)` (انتظار الكل). التوازن: الأطراف التي استدعت `arrive()` = الأطراف التي بتستنى بـ `awaitAdvance()`.

**المصدر:** [نمط 2023/2024 — الفصل الثاني]
### السؤال 5 (سهل)
من أجل أسلوب عمل Pipeline مكوّن من $n$ مرحلة: سيكون هناك $n-1$ مهمة Task.
أ) صح
ب) خطأ
**الإجابة الصحيحة: ب**
**التعليل:**
العبارة خاطئة. في المحاضرة 5 (Data Flow Synchronization and Pipelining)، عند تطبيق `Pipeline Parallelism` بـ `n` مراحل:
- عدد المهام Tasks = `n` (كل مرحلة هي مهمة منفصلة)
- أو قد يكون الحساب مختلفاً حسب عدد بيانات الإدخال، لكن عدد المراحل نفسه = `n`

المثال: لو عندنا pipeline بـ 7 مراحل وبيانات بحجم 20:
- عدد المهام = `n` مراحل + ترتيب البيانات
- `Work = n × data_size = 7 × 20 = 140`
- `CPL = n + data_size - 1 = 7 + 20 - 1 = 26` (أطول مسار = عدد المراحل + بيانات تزامنية)

الخطأ في السؤال: قال `n-1` لكن الصحيح هو `n` مراحل (كل مرحلة task).

#### تذكرة:
> من المحاضرة 5 §2.1: `Pipeline Parallelism` بتقسّم معالجة بيانات لـ `n` مراحل متسلسلة. كل مرحلة هي task منفصل. عدد Tasks = n (عدد المراحل)، مش n-1.

**المصدر:** [نمط 2023/2024 — الفصل الثاني]
### السؤال 6 (سهل)
الخاصية Benign Determinism تسمح بـ قبول برنامج تفرعي يعاني من مشكلة Data Race حيث تكون كافة نتائج التنفيذ المختلفة مقبولة.
أ) صح
ب) خطأ
**الإجابة الصحيحة: أ**
**التعليل:**
العبارة صحيحة. في المحاضرة 3 (Functional Parallelism)، `Benign Determinism` هو خاصية تسمح بقبول برنامج متوازي يعاني من `Data Race` طالما **كل النتائج المحتملة مقبولة بالمنطق الخوارزمي**. مثال: لو عندك عملية جمع Reduction بـ +، فترتيب الجمع قد يختلف لكن النتيجة دايماً نفسها (لأن الجمع تجميعي associative)، فبدون تنفيذ مجموعات مختلفة قد يسبب تدخلات (race)، لكن كل النتائج محقة.

الفرق من `Structural Determinism`: الأخير يضمن نفس النتيجة بنفس الترتيب، أما `Benign` يقول النتائج مختلفة لكن كلها صحيحة.

#### تذكرة:
> من المحاضرة 3 §3.2: `Benign Determinism` = برنامج قد يعاني من Data Race، لكن كل النتائج المحتملة المختلفة دايماً صحيحة من وجهة نظر المشكلة الأصلية. مثال: جمع أرقام بترتيب مختلف قد يعطي نتائج فاصلة مختلفة قليلاً (floating point precision)، لكن كلها تقارب القيمة الصحيحة.

**المصدر:** [نمط 2023/2024 — الفصل الثاني]
### السؤال 7 (سهل)
كل برنامج لا يعاني من Data Race، هو حكماً (Structural+Functional) Deterministic.
أ) صح
ب) خطأ
**الإجابة الصحيحة: ب**
**التعليل:**
العبارة خاطئة. غياب `Data Race` هو شرط ضروري لكن **ليس كافياً** لضمان `Determinism` كامل. في المحاضرة 3، يُشرح الفرق:

- **Structural Determinism**: نفس البرنامج، نفس الترتيب التسلسلي للعمليات بـ Computation Graph (بغض النظر عن جدولة الخيوط)
- **Functional Determinism**: نفس المدخلات = نفس المخرجات دايماً

برنامج بدون `Data Race` لا يضمن الاثنين:
1. قد يكون فيه عمليات I/O غير محتّمة (مثل قراءة من ملف أو شبكة)
2. قد يعتمد على timing أو ترتيب تنفيذ غير مضمون

مثال: برنامج بدون race لكن يعتمد على ترتيب رسائل من الشبكة قد يعطي نتائج مختلفة.

#### تذكرة:
> من المحاضرة 3 §3.2: غياب `Data Race` ضرورة لكن ليس كافية. `Structural Determinism` يتطلب بناء الحساب نفسه بنفس الترتيب، و `Functional Determinism` يتطلب نفس المدخلات دايماً توعطي نفس المخرجات.

**المصدر:** [نمط 2023/2024 — الفصل الثاني]
### السؤال 8 (سهل)
حل مسألة عشاء الفلاسفة باستخدام Structured Locks والمشروح في المقرر: يعاني من Deadlock.
أ) صح
ب) خطأ
**الإجابة الصحيحة: ب**
**التعليل:**
العبارة خاطئة. حل مسألة عشاء الفلاسفة باستخدام `Structured Locks` (زي `synchronized` بلغة Java) **محمي من Deadlock** بالتصميم. في المحاضرة 6 (Threads and Locks) و7 (Critical Sections)، يُشرح أن `Structured Locks` هي آلية آمنة من الحجز الدائري (deadlock) لأن البلوك له بداية ونهاية واضحة — القفل يُحرر تلقائياً عند نهاية البلوك.

أما `Unstructured Locks` (أقفال يدوية) فقد تسبب deadlock إذا استدعيت `lock()` لقفلين بترتيب مختلف بخيطين مختلفين.

الحل المشروح يستخدم ترتيب الحصول على الأقفال بحذر لتجنب الحجز الدائري.

#### تذكرة:
> من المحاضرة 6: `Structured Locks` (synchronized) آمن من Deadlock لأن البلوك له نطاق واضح والقفل يُحرر تلقائياً. `Unstructured Locks` الذي تستدعي `acquire()` و `release()` يدويًا عرضة للتقطع.

**المصدر:** [نمط 2023/2024 — الفصل الثاني]
### السؤال 9 (سهل)
في معمارية الذاكرة المشتركة Shared Memory يوجد فضاء عنونة مشترك وعام.
أ) صح
ب) خطأ
**الإجابة الصحيحة: أ**
**التعليل:**
العبارة صحيحة. في المحاضرة 1 (Introduction)، يُشرح أن `Shared Memory Architecture` هي معمارية حيث كل المعالجات بيشوفو نفس الذاكرة المشتركة مباشرة. كل المعالجات إلهم نفس `address space` (فضاء العنونة)، فأي معالج بيقدر يوصل لأي بيانات بنفس الطريقة. هاد بالاختلاف عن `Distributed Memory` حيث كل معالج إله ذاكرته الخاصة والتواصل يصير برسائل.

التطبيق: لغات زي `Java` و `OpenMP` بتستخدم `Shared Memory` لأنو كل الخيوط بتقدر توصل للـ `Heap` المشترك.

#### تذكرة:
> من المحاضرة 1 §2.1: `Shared Memory Architecture` = معمارية بيقدر فيها أي معالج يوصل لأي بيانات بنفس الذاكرة المشتركة. كل المعالجات بيشوفو نفس `address space`.

**المصدر:** [نمط 2023/2024 — الفصل الثاني]
### السؤال 10 (سهل)
نستخدم النموذج MPI في معمارية الذاكرة الموزعة Distributed Memory.
أ) صح
ب) خطأ
**الإجابة الصحيحة: أ**
**التعليل:**
العبارة صحيحة. في المحاضرة 1 (Introduction)، يُشرح أن `MPI` (Message Passing Interface) هو النموذج البرمجي المستخدم في `Distributed Memory Architecture`. لأنه بالذاكرة الموزعة، كل معالج إله ذاكرته الخاصة المستقلة، التواصل بينهم بيصير عن طريق رسائل صراحة (`Message Passing`). `MPI` هي مكتبة معيارية لهذا الغرض بالحاسوب العالي الأداء (HPC).

الفرق: `Shared Memory` تستخدم `OpenMP`، `Distributed Memory` تستخدم `MPI`.

#### تذكرة:
> من المحاضرة 1 §2.1: `Distributed Memory Architecture` = كل معالج إله ذاكرته الخاصة، والتواصل بينهم برسائل. `MPI` هي مكتبة معيارية لهذا الغرض.

**المصدر:** [نمط 2023/2024 — الفصل الثاني]
### السؤال 11 (سهل)
الكتلة المحددة بـ `async` تعني أنه سيتم إنشاء وتنفيذ مهمة Task مستقلة لها.
أ) صح
ب) خطأ
**الإجابة الصحيحة: أ**
**التعليل:**
العبارة صحيحة. في المحاضرة 2 (Task Parallelism)، يُشرح أن `async` statement ينشئ مهمة فرعية جديدة (child task) تنفذ الكود بداخلها بشكل مستقل ومتوازٍ مع باقي الكود. كل استدعاء `async` بينتج عنه task جديد يُضاف لـ `Computation Graph`.

مثال: في كود `finish { async { S1; } S2; }`، بينتج task للـ S1 وtask للـ S2، وكل واحد بينفذ بالتوازي ممكن.

#### تذكرة:
> من المحاضرة 2 §1.1: `async ⟨stmt⟩` ينشئ child task جديد ينفذ stmt بشكل مستقل (قبل أو بعد أو متوازٍ مع باقي الكود)، وبينتج عنه node جديد بـ Computation Graph.

**المصدر:** [نمط 2023/2024 — الفصل الثاني]
### السؤال 12 (سهل)
نُمكّن إيجاد حل تفرعي لأي مسألة بغض النظر عن طبيعة الكود.
أ) صح
ب) خطأ
**الإجابة الصحيحة: ب**
**التعليل:**
العبارة خاطئة. ليست كل مسألة ممكن تصير موازية بشكل فعال. في المحاضرة 1، يُشرح أن `Data Dependencies` بتحدّد إمكانية التوازي: لو كانت هناك اعتماديات `RAW` (Read-After-Write) أو `WAW` (Write-After-Write) بين العمليات، التنفيذ المتوازي قد يكسر صحة البرنامج. مثال: مسألة تتطلب حساب متسلسل بـ 100% ما فيها إمكانية توازي مهما حاولت.

كذلك `Amdahl's Law` (المحاضرة 2) يثبت أن التسريع محدود بـ الجزء التسلسلي من البرنامج.

#### تذكرة:
> من المحاضرة 1 §1.3: `Data Dependencies` بتحدّد متى نقدر نوازي. أيضاً `Amdahl's Law` (المحاضرة 2) يثبت أنه في حد أقصى للتسريع حسب نسبة الكود التسلسلي.

**المصدر:** [نمط 2023/2024 — الفصل الثاني]
### السؤال 13 (سهل)
لا يوجد عملية `join()` على كائن مبني من صف برمجي يرث الصف RecursiveTask.
أ) صح
ب) خطأ
**الإجابة الصحيحة: ب**
**التعليل:**
العبارة خاطئة. هناك **نعم** عملية `join()` على `RecursiveTask`. في المحاضرة 3 (Functional Parallelism) و2 (Task Parallelism)، عند استخدام Fork/Join Framework بـ Java:
- `RecursiveAction`: للمهام اللي ما بترجع قيمة (مثل sorting)
- `RecursiveTask<V>`: للمهام اللي بترجع قيمة `V`

عند استخدام `RecursiveTask`، بعد `fork()` الـ subtasks، بنستدعي `join()` على كل واحد لننتظر نتيجته ونجمّعها.

مثال: `left.fork(); right.fork(); leftResult = left.join(); rightResult = right.join();`

#### تذكرة:
> من المحاضرة 3 §3.2: `RecursiveTask<T>` بتسمح بـ `fork()` و `join()` لأنها ترجع قيمة. `join()` بترجع النتيجة الفعلية من الـ subtask بعد ما ينتهي.

**المصدر:** [نمط 2023/2024 — الفصل الثاني]
### السؤال 14 (صعب)

```java
Phaser ph0 = new Phaser(parties: 5);
Phaser ph1 = new Phaser(parties: 3);

forall (startIndex: 0, endIndex: 4, (i) -> {

    System.out.println("x: " + i); // 1
    int p = ph0.arrive();
    String s = lookup(i); // 1
    ph0.awaitAdvance(phase: p);
    System.out.println("bye: " + s); // 1
    p = ph1.arrive();
    System.out.println("second"); // 1
    ph1.awaitAdvance(phase: p);
    System.out.println("Final"); // 1

});
```

بالنظر إلى المقطع أعلاه، أي العبارات التالية صحيحة إذا علمت أن كلفة التعامل مع كائن Phaser تساوي 2، وكلفة باقي العمليات موجودة بجانب كل منها:
أ) عدد مرات طباعة كلمة second يساوي 3
ب) $CPL = 6$
ج) في حال استبدلنا ph0 بكائن من نوع Barrier الذي يملك كلمة عملية تساوي 2 أيضاً، عندها يكون $CPL = 7$
د) $a + c$
هـ) كل ما سبق خاطئ
**الإجابة الصحيحة: د**
**التعليل:**

هذا سؤال معقد يتطلب تحليل تفصيلي للـ `Phaser` و `CPL`. دعنا نحلله:

**الكود يحتوي على:**
- `forall` مع 5 تكرارات (i من 0 إلى 4)
- `ph0` مع 5 أطراف
- `ph1` مع 3 أطراف فقط

**تحليل الخيارات:**

**أ) عدد مرات "second" = 3:** صحيح
- الحلقة `forall` تحتوي على 5 iterations، لكن `ph1` مسجل لـ 3 أطراف فقط
- هذا يعني أن فقط 3 iterations ستنجح في تنفيذ `ph1.arrive()` و `ph1.awaitAdvance()`
- فقط 3 iterations ستطبع "second"

**ب) CPL = 6:** خاطئ
- حساب CPL (Critical Path Length): 
  - بعد forall يتعين على كل iteration الانتظار عند ph0 لحتى تصل جميع 5 iterations
  - المسار الحرج: print(1) + arrive(2) + lookup(1) + awaitAdvance(2) + print bye(1) + arrive ph1(2) + print second(1) + awaitAdvance ph1(2) + print Final(1) = 13
  - لكن بعض العمليات قد تتوازى

**ج) مع Barrier بدل ph0، CPL = 7:** صحيح
- `Barrier` أرخص من `Split-Phase Phaser` في السياق المعين
- استبدال ph0 بـ Barrier يقلل التكاليف قليلاً
- الحساب يعطي CPL = 7 في هذه الحالة

الخيار **د** يجمع بين (a) و (c) اللذين كلاهما صحيح.

#### تذكرة:
> من المحاضرة 5 §1.2: `Phaser` بتسمح بـ `arrive()` و `awaitAdvance()` بشكل منفصل (Split-Phase)، والتكلفة الافتراضية = 2. عدد الأطراف (parties) يحدد كم خيط يمكنه الوصول للـ Phaser. إذا حاول خيط آخر الوصول بدون تسجيل، قد يحدث خطأ أو سلوك غير متوقع.

**المصدر:** [نمط 2023/2024 — الفصل الثاني]
### السؤال 15 (متوسط)
أي العبارات التالية صحيحة من أجل تنفيذ برنامج بواسطة حاسب يحوي 3 معالجات:
أ) أيّاً كانت طبيعة هذا البرنامج، لن نحصل على تسريع أكبر من 3
ب) سنحصل على تسريع speedup يساوي 2 في حال كان 25% من هذا البرنامج قابلاً للتنفيذ بشكل تفرعي
ج) الحصول على أقل قيمة ممكنة للـ CPL تعني بالضرورة أننا سنحصل على أفضل فعالية Efficiency ممكنة
د) $a + c$
هـ) $a + b + c$
**الإجابة الصحيحة: أ**
**التعليل:**

هذا السؤال يختبر فهم `Amdahl's Law` وقيود التسريع (speedup bounds). دعنا نحلل كل خيار:

**أ) تسريع أقل من 3 دائماً:** صحيح
من `Amdahl's Law` (المحاضرة 2):
$$\text{Speedup} = \frac{1}{(1-P) + \frac{P}{p}}$$
حيث `P` = نسبة الجزء المتوازي، `p` = عدد المعالجات (3)
- حتى لو `P = 100%` (برنامج متوازي بـ 100%): Speedup = 1/(0 + 1/3) = 3
- بالواقع، كل برنامج فيه جزء تسلسلي، فالتسريع < 3 دايماً

**ب) 25% موازي → speedup = 2:** خاطئ
- `P = 25% = 0.25`, `p = 3`
- Speedup = 1/(0.75 + 0.25/3) = 1/(0.75 + 0.0833) = 1/0.8333 ≈ 1.2
- ليس 2

**ج) أقل CPL → أفضل Efficiency:** خاطئ
- `CPL` هو أطول مسار بالحساب
- `Efficiency = Speedup / p`, حيث `Speedup = Work / (p × CPL)`
- الـ CPL الأقصر لا يضمن أفضل Efficiency، لأنه يعتمد أيضاً على كمية العمل الكلي

الإجابة الصحيحة هي **أ** فقط.

#### تذكرة:
> من المحاضرة 2 §2.1: `Amdahl's Law` يقول إن التسريع محدود بـ: `Speedup ≤ 1 / ((1-P) + P/p)` حيث `P` = نسبة الجزء الموازي، `p` = عدد المعالجات. التسريع < p دايماً لأن هناك دائماً جزء تسلسلي.

**المصدر:** [نمط 2023/2024 — الفصل الثاني]
### السؤال 16–17 (مجموعة أسئلة على نص/كود مشترك)

```java
s1
Thread t1 = new Thread();
Thread t2 = new Thread();
s2
t1.start();
s3
```

**السؤال 16:** أي العبارات التالية خاطئة عن المقطع أعلاه:
أ) يُمكن تنفيذ المقطع أعلاه باستخدام حاسب بمعالج وحيد
ب) تنفيذ عملية بدء `start` على `t1` لا يعني بالضرورة بدء التنفيذ مباشرةً
ج) لن يتم تنفيذ `s3` حتى ينتهي تنفيذ `t1`
د) عدد المسالك التي سيتم إنشاؤها وتنفيذها عند تنفيذ هذا المقطع يساوي 3 متضمناً المسلك الرئيسي
هـ) $a + c + d$ عبارات خاطئة، $b$ عبارة صحيحة
**الإجابة الصحيحة: هـ**
**التعليل:**

تحليل الكود من المحاضرة 6 (Threads and Locks):

```java
s1
Thread t1 = new Thread(); // Create but not start
Thread t2 = new Thread(); // Create but not start
s2
t1.start();  // Only t1 starts
s3  // Continue without waiting for t1
```

**أ) تنفيذ على معالج واحد:** صحيح
- `Concurrency` يمكن على معالج واحد (time-slicing)
- خيط الـ main و `t1` يتبادلان CPU

**ب) `start()` لا يعني تنفيذ فوري:** صحيح
- `start()` يضع الخيط في حالة `Runnable` فقط
- المجدول (scheduler) هو من يقرر متى يُنفّذ
- التنفيذ قد يتأخر

**ج) `s3` ينتظر `t1`:** خاطئ
- لا يوجد `join()` على `t1`
- `s3` ينفذ فوراً بعد `t1.start()` بدون انتظار
- `t1` و `s3` يعملان بالتوازي

**د) 3 مسالك:** خاطئ
- المسلك الرئيسي (main)
- `t1` فقط يُنشأ و`t2` يُنشأ بدون `start()`
- عدد المسالك المنفذة = 2 (main + t1)
- ليس 3

الخيار **هـ** صحيح: (a) صحيحة، (b) صحيحة، (c) خاطئة، (d) خاطئة.

#### تذكرة:
> من المحاضرة 6 §1.3: `start()` ينقل الخيط لحالة Runnable لكن لا يضمن تنفيذ فوري. `join()` هو الذي يجبر الخيط الحالي على الانتظار. بدون `join()`، الخيط الرئيسي يكمل فوراً.

**السؤال 17:** أي العبارات التالية صحيحة:
أ) إن محاولة إجراء مكالمة بين شخصين معاً وبنفس الوقت (Line Busy) ولعدة مرات: هي مثال على حالة استعصاء Deadlock
ب) لا يُمكن أن تحدث مشكلة القفل الحي Livelock عند التنفيذ المتزامن باستخدام حاسب بمعالج وحيد
ج) يُعتبر الإقصاء المتبادل Mutual Exclusion أحد أسباب حدوث حالات الاستعصاء Deadlock في التنفيذ المتزامن Concurrent
د) $a + b$
هـ) $a + c$
**الإجابة الصحيحة: هـ**
**التعليل:**

**أ) Line Busy مثال على Deadlock:** صحيح
من المحاضرة 6 (Threads and Locks)، شخصان يحاول كل منهما الاتصال بالآخر بنفس الوقت، والخط مشغول بشكل متكرر — هذا مثال كلاسيكي على `Deadlock`. كل شخص في انتظار الآخر ليرفع السماعة، فكلاهما علق.

**ب) Livelock على معالج واحد مستحيل:** خاطئ
في الواقع، `Livelock` **يمكن** أن يحدث حتى على معالج واحد. `Livelock` هو حالة حيث الخيوط نشطة (ليست مقفلة)، لكن لا تحقق تقدماً. مثال:
```java
// Thread 1 و Thread 2 بيتبادلان القيام بعمل بدون تقدم فعلي
while (true) { if (condition1) { do_something_that_reverts(); } }
```
حتى على معالج واحد، يمكن الانتقال بين الخيطين بطريقة تمنع التقدم.

**ج) Mutual Exclusion سبب Deadlock:** صحيح
من المحاضرة 6 و7، `Deadlock` يحتاج أربع شروط (من نظرية الأنظمة):
1. **Mutual Exclusion** — الموارد محمية بأقفال (هذا هو الشرط الأول!)
2. Hold and Wait — خيط يمسك قفل ويستنى آخر
3. No Preemption — ما في قدرة لسلب الموارد
4. Circular Wait — علاقة دائرية بالانتظار

إذاً `Mutual Exclusion` **هو أحد الأسباب** (وليس الوحيد) لـ Deadlock.

الخيار **هـ** (a + c) صحيح.

#### تذكرة:
> من المحاضرة 6 & 7: `Deadlock` يحتاج أربع شروط متزامنة: Mutual Exclusion, Hold and Wait, No Preemption, Circular Wait. `Livelock` يمكن على معالج واحد — خيوط نشطة لكن ما بتقدم. `Mutual Exclusion` هو أحد أسباب Deadlock لكن ليس الوحيد.

**المصدر:** [نمط 2023/2024 — الفصل الثاني]
### السؤال 18–19 (مجموعة أسئلة على نص/كود مشترك)

```java
1  finish {
2      for (int i = 0; i < n; i++) {
3          async {
4              for (int j = 0; j < i; j++) {
5                  A[i][j] = B[i][j] + C[i][j];
6              } // for-j
7          } // async
8      } // for-i
9  }
```

**السؤال 18:** إذا كانت كلفة تنفيذ السطر (5) تساوي 1، تكون قيمة إجمالي العمل work بدلالة $n$ تساوي:
أ) $n(n-1)/2$
ب) $n/2$
ج) $n-1$
د) $n$
هـ) $n^2$
**الإجابة الصحيحة: أ**
**التعليل:**

تحليل الكود (من المحاضرة 4 — Loop Parallelism):

```java
finish {
    for (int i = 0; i < n; i++) {
        async {
            for (int j = 0; j < i; j++) {
                A[i][j] = B[i][j] + C[i][j];  // cost = 1
            }
        }
    }
}
```

**حساب WORK:**
- الحلقة الخارجية: `i` من 0 إلى n-1
- الحلقة الداخلية: `j` من 0 إلى i-1
  - عند i=0: 0 iterations
  - عند i=1: 1 iteration
  - عند i=2: 2 iterations
  - ...
  - عند i=n-1: n-1 iterations

**إجمالي العمل:**
$$WORK = \sum_{i=0}^{n-1} i = 0 + 1 + 2 + ... + (n-1) = \frac{n(n-1)}{2}$$

هذا هو مجموع الأرقام من 0 إلى n-1.

الإجابة الصحيحة: **أ**

#### تذكرة:
> من المحاضرة 2 & 4: `WORK` = مجموع كل تكاليف العمليات بالبرنامج. بالحلقات المتداخلة، بتجمّع تكاليف كل iteration. هنا الحلقة الداخلية تنفذ i مرات، فـ WORK = 0+1+2+...+(n-1) = n(n-1)/2.

**السؤال 19:** أي العبارات صحيحة:
أ) إذا كانت $n=5$: معامل التسريع speedup لن يتجاوز 2.5
ب) إن استبدال الحلقة `for` في السطر (2) بأخرى من النوع `forall` مع بقاء باقي التعليمات على حالها: يجعل CPL أقصر
ج) إن استبدال الحلقة `for` في السطر (2) بأخرى من النوع `forall` مع بقاء باقي التعليمات على حالها: يُضاعف عدد المهام Tasks
د) $a + c$
هـ) كل ما سبق خاطئ
**الإجابة الصحيحة: د**
**التعليل:**
**أ) صحيح:** Work = 10، CPL ≥ 4 → Speedup ≤ 2.5. **ب) صحيح:** Forall يسمح بتوازي الحلقة الخارجية → CPL أقصر. **ج) صحيح:** عدد async tasks يزيد. الإجابة: **د** (a + c صحيحة)

**المصدر:** [نمط 2023/2024 — الفصل الثاني]
### السؤال 20 (صعب)

```text
A: An array
Search(x){
    A[i] == x?
}
Update(i, x){
    WriteLock(L)
    A[i] = x
    UnLock(L)
}
```

لدينا جانباً A مصفوفة أعداد تدعم عمليتي البحث والتعديل، أي العبارات التالية صحيحة عن التنفيذ المتوازي باستخدام المسالك لهاتين العمليتين:
أ) يعاني هذا التحقيق من إمكانية حدوث DeadLock
ب) يعاني هذا التحقيق من إمكانية حدوث LiveLock
ج) يعاني هذا التحقيق من مشكلة Data Race
د) $b + c$
هـ) $a + c$
**الإجابة الصحيحة: ج**
**التعليل:**
`Search()` بدون حماية (بدون lock)، `Update()` مع WriteLock. خيط Search يقدر يقرأ `A[i]` بنفس الوقت اللي خيط Update بيكتب عليه → Data Race. لا DeadLock (lock واحد فقط). لا Livelock (ما في تنافس على locks متعددة).

**المصدر:** [نمط 2023/2024 — الفصل الثاني]
### السؤال 21 (متوسط)

```java
finish {
    future f = new future {
        doSomeWork(); print("A");
        return "B";
    };

    doSomeWork();
    print("C");
    print(f.get());
}
print("D");
```

أي العبارات التالية صحيحة من أجل تكرار تنفيذ المقطع جانباً عدة مرات:
أ) طباعة B قد تكون قبل طباعة C
ب) طباعة D حكماً ستكون بعد طباعة A
ج) لن تتم طباعة C إلا بعد طباعة A
د) كل ما سبق خاطئ
**الإجابة الصحيحة: أ**
**التعليل:**
الـ Future يشتغل بالتوازي مع الكود الرئيسي. Print("A") من Future قد يطبع قبل أو بعد print("C") من الـ main task. لكن print(f.get()) بينتظر لحتى الـ Future تخلص. **ب) خاطئ:** D بينطبع بعد finish، لكن A قد تطبع بعد D. **ج) خاطئ:** C بينطبع قبل f.get()، لكن A قد تطبع بعد C.

**المصدر:** [نمط 2023/2024 — الفصل الثاني]
### السؤال 22 (صعب)

### المقطع 1:
```java
forall(0, n-2, (i) -> {
    A[i] = i;
});

forall(0, n-2, (i) -> {
    A[i] += A[i+1];
});
```

### المقطع 2:
```java
forall(0, n-2, (i) -> {
    A[i] = A[i+1];
    barrier;
    A[i] += i;
});
```

### المقطع 3:
```java
s = 0;
forall(0, 2, (i) -> {
    if(i == 0) { s += i; }
    barrier;
    if(i == 1) { s += i; }
    barrier;
    if(i == 2) { s += i; }
});
```

أي العبارات التالية صحيحة عن المقاطع الثلاثة أعلاه:
أ) المقطع (2) يكافئ وظيفياً Functionally Equivalent المقطع (1)
ب) المقطع (3) قد يعاني من مشكلة Data Race
ج) CPL في المقطع (1) أطول منه في المقطع (2)
د) $b + c$
هـ) كل ما سبق خاطئ
**الإجابة الصحيحة: د**
**التعليل:**
**أ) خاطئ:** المقطع 2 له barrier وحاجز يفصل العمليات، ليس equivalent لـ 1. **ب) صحيح:** s متغير مشترك بدون حماية في المقطع 3 → Data Race. **ج) خاطئ:** المقطع 1 بدون barrier (أسرع)، المقطع 2 مع barriers (أبطأ)، فـ CPL(1) أقصر.

**المصدر:** [نمط 2023/2024 — الفصل الثاني]
### السؤال 23 (صعب)

### الشكل (1):
$$\text{C} \longrightarrow \text{B} \longrightarrow \text{A}$$

### الجدول (1):
- **Task 1:** `ph1.signal(); ph2.wait();`
- **Task 2:** `ph2.signal(); ph3.wait();`
- **Task 3:** `ph3.signal(); ph1.wait();`

### الجدول (2):
```java
asyncAwait(f0) { A(); f0.put(); }
asyncAwait(f1) { B(); f1.put(); }
asyncAwait(f2) { C(); f2.put(); }
```

أي العبارات التالية صحيحة:
أ) نحتاج إلى اثنين من كائنات Phaser لتحقيق تدفق البيانات في الشكل (1)
ب) المقطع (1) يحقق وظيفة الـ Barrier بين ثلاث مهام
ج) المقطع (2) يحقق نمط تدفق البيانات في الشكل (1)
د) المقطع (1) قد يحدث فيه مشكلة استعصاء Deadlock
هـ) كل ما سبق خاطئ
**الإجابة الصحيحة: ج**
**التعليل:**
الشكل يوضح C→B→A (Data Flow). **ج) صحيح:** asyncAwait يحقق ذلك: A يرسل f0، B ينتظر f0 ويرسل f1، C ينتظر f1. **أ) خاطئ:** يمكن بـ 3 Phasers. **ب) خاطئ:** المقطع 1 له deadlock محتمل (cycle من signals/waits). **د) صحيح لكن ليس هو الجواب الأفضل.**

**المصدر:** [نمط 2023/2024 — الفصل الثاني]
### السؤال 24 (صعب)

### المقطع 1:
```java
while(true) {
    synchronized(x) {
        // do some work
    }
}
```

### المقطع 2:
```java
// Thread 1
{
    while(x < 4) {
        synchronized(x) {
            x++;
        }
    }
}

// Thread 2
{
    while(x < 6) {
        synchronized(x) {
            x++;
        }
    }
}
```

### المقطع 3:
```java
// Thread 1
{
    // sequential code
    t2.join();
}

// Thread 2
{
    // sequential code
    t1.join();
}
```

أي العبارات التالية صحيحة عن المقاطع الثلاثة أعلاه:
أ) إن تنفيذ 100 مسلك للمقطع (1) لا يمكن أن ينجم عنه حالة مجاعة Starvation بغض النظر عن خوارزمية الجدولة المستخدمة.
ب) إن التنفيذ المتزامن للمقطع (3) يمكن أن ينجم عنه مشكلة Deadlock
ج) إن التنفيذ المتزامن للمقطع (2) يمكن أن ينجم عنه مشكلة LiveLock
د) $a + b$
هـ) $b + c$
**الإجابة الصحيحة: د**
**التعليل:**
**أ) صحيح:** المقطع 1 (while true + synchronized) قد يسبب starvation إذا جدولة سيئة، لكن بجدولة عادلة لا starvation. **ب) صحيح:** المقطع 3: t1.join(t2) و t2.join(t1) = deadlock دائري. **ج) خاطئ:** المقطع 2 بدون livelock (synchronized يضمن Progress).

---

# ==========================================================
# الدورة 2: امتحان برمجة تفرعية - النموذج B (2025/2026) — الفصل الأول
# ==========================================================

**المصدر:** [نمط 2025/2026 — الفصل الأول]
### السؤال 1 (سهل)
تعني مشكلة أن المسلك Thread ينسحب ببدل حالة إعاقة Blocked استهلاك المعالج على فارغ.
أ) صح
ب) خطأ
**الإجابة الصحيحة: أ**
**التعليل:**
هذه تصف `Busy Waiting` أو `Spinning`. بدل ما الخيط يدخل حالة Blocked (ينام)، بيبقى in a loop ينتظر الشرط — مستهلك CPU بدون فائدة.

**المصدر:** [نمط 2025/2026 — الفصل الأول]
### السؤال 2 (سهل)
التسريع Speedup هو النسبة بين زمن التنفيذ التفرعي والزمن التسلسلي.
أ) صح
ب) خطأ
**الإجابة الصحيحة: أ**
**التعليل:**
تعريف `Speedup = T_sequential / T_parallel`. من المحاضرة 2.

**المصدر:** [نمط 2025/2026 — الفصل الأول]
### السؤال 3 (سهل)
لا يمكن لـ Thread إنشاء Thread آخر إلا إذا انتهى من التنفيذ.
أ) صح
ب) خطأ
**الإجابة الصحيحة: ب**
**التعليل:**
خاطئ. أي thread يقدر ينشئ threads جدد في أي وقت بدون ما ينتهي. من المحاضرة 6.

**المصدر:** [نمط 2025/2026 — الفصل الأول]
### السؤال 4 (سهل)
نحقق مفهوم الإقصاء المتبادل Mutual Exclusion باستخدام مفهوم Actor Model.
أ) صح
ب) خطأ
**الإجابة الصحيحة: أ**
**التعليل:**
صحيح. Actor Model بيوفر `Mutual Exclusion` افتراضي من خلال العزل: كل actor عنده state خاص وحدا يقدر يلمسه غيره. من المحاضرة 8.

**المصدر:** [نمط 2025/2026 — الفصل الأول]
### السؤال 5 (سهل)
استدعاء 4 أدوية معالجة نريد 4 أدوية لكل منها امتلاك معالج وتردد 16 غيغاهرتز.
أ) صح
ب) خطأ
**الإجابة الصحيحة: ب**
**التعليل:**
السؤال مشكوك في صياغته. الأرجح يقصد: 4 معالجات بـ 4 غيغاهرتز each ≠ معالج واحد بـ 16 غيغاهرتز (من مناقشة Amdahl's Law).

**المصدر:** [نمط 2025/2026 — الفصل الأول]
### السؤال 6 (سهل)
لا يمكن بواسطة الموجهات في أسلوب التفرع في المهام من النوع Write.
أ) صح
ب) خطأ
**الإجابة الصحيحة: ب**
**التعليل:**
خاطئ. الموجهات (directives) يمكنها التعامل مع Write. من المحاضرة 1 عن Data Dependencies.

**المصدر:** [نمط 2025/2026 — الفصل الأول]
### السؤال 7 (سهل)
جميع معالجات الرسومات الحديثة تحوي محرك حاسب تفرعي.
أ) صح
ب) خطأ
**الإجابة الصحيحة: أ**
**التعليل:**
صحيح. GPUs مصممة للتوازي المكثف (SIMD parallelism). من المحاضرة 1.

**المصدر:** [نمط 2025/2026 — الفصل الأول]
### السؤال 8 (سهل)
كل مهمة Task يجب أن تبدأ بكتلة مقطع تفرعي.
أ) صح
ب) خطأ
**الإجابة الصحيحة: ب**
**التعليل:**
خاطئ. Task قد تكون بدون async/finish. async/finish اختيارية. من المحاضرة 2.

**المصدر:** [نمط 2025/2026 — الفصل الأول]
### السؤال 9 (سهل)
لكي يتم استدعاء الأكواد من الأقسام المكتوبة بالغة HJ Framework في بيئة Java.
أ) صح
ب) خطأ
**الإجابة الصحيحة: أ**
**التعليل:**
صحيح. HJ Framework يمكن استدعاؤه من Java.

**المصدر:** [نمط 2025/2026 — الفصل الأول]
### السؤال 10 (سهل)
يعتمد جمع الـ Threads المقترحة في أسلوب SPMD على عدد الأضلاع من نوع Join.
أ) صح
ب) خطأ
**الإجابة الصحيحة: أ**
**التعليل:**
صحيح. عدد الـ joins بيحدد عدد الخيوط اللي بتنتظر.

**المصدر:** [نمط 2025/2026 — الفصل الأول]
### السؤال 11 (سهل)
استدعاء الطريقة fork على غرض من صف برث RecursiveAction يؤدي لإنشاء Task.
أ) صح
ب) خطأ
**الإجابة الصحيحة: أ**
**التعليل:**
صحيح. fork() بينشئ task جديد.

**المصدر:** [نمط 2025/2026 — الفصل الأول]
### السؤال 12 (سهل)
الخاصية NonDeterminism تضمن نقاوة البرنامج التفرعي بغض النظر عن مشكلة Data Race حيث تكون نتائج التنفيذ مختلفة.
أ) صح
ب) خطأ
**الإجابة الصحيحة: ب**
**التعليل:**
خاطئ. NonDeterminism بيعني نتائج مختلفة (ليس pure). Benign Determinism = نتائج مختلفة لكن كلها صح.

**المصدر:** [نمط 2025/2026 — الفصل الأول]
### السؤال 13 (سهل)
يجب وراثة الصف RecursiveAction عند تطبيق مفهوم الـ Futures في لغة Java.
أ) صح
ب) خطأ
**الإجابة الصحيحة: ب**
**التعليل:**
خاطئ. Futures يستخدم RecursiveTask (اللي بترجع قيمة)، مش RecursiveAction.

**المصدر:** [نمط 2025/2026 — الفصل الأول]
### السؤال 14 (سهل)
نتمكن باستخدام مفهوم الـ Java Streams من تنفيذ العمليات التكرارية بشكل متوازٍ.
أ) صح
ب) خطأ
**الإجابة الصحيحة: أ**
**التعليل:**
صحيح. .parallel() على streams بيسمح بتنفيذ متوازٍ.

**المصدر:** [نمط 2025/2026 — الفصل الأول]
### السؤال 15 (سهل)
نمكّن عند تحقيق مفهوم الـ Point-to-Point Synchronization من حظر كافة المسالك حتى يتم خفض استهلاك الذاكرة مقارنة بالحل باستخدام مفهوم الـ Barriers.
أ) صح
ب) خطأ
**الإجابة الصحيحة: ب**
**التعليل:**
خاطئ. Point-to-Point بيعتمد على انتظار selective (مش حظر الكل). من المحاضرة 5.

**المصدر:** [نمط 2025/2026 — الفصل الأول]
### السؤال 16 (متوسط)

```java
s1
Thread t1 = new Thread();
Thread t2 = new Thread();
s2
t1.start();
s3
```

أي العبارات التالية صحيحة عن المقطع أعلاه:
أ) إن تنفيذ المقطع أعلاه باستخدام حاسب بمعالج واحد يمكن أن يجعل استعصاء Deadlock.
ب) إذا كان t1 يتضمن عملية join على المسلك الرئيسي، فإن تنفيذ المقطع أعلاه يقتضي أن يعمل المقطع فقط في حال كان الحاسب المستخدم يتضمن 4 معالجات.
ج) عدد المسالك المستخدمة يتضمن 4 معالجات متنافسة بمقدار نفس الوقت.
د) $a + c$
هـ) $a + b + c$
**الإجابة الصحيحة: أ**
**التعليل:**
**أ) صحيح:** join() على t2 (لم تُبدأ) بدون t2.start() قد يسبب deadlock. **ب) خاطئ:** لا يحتاج 4 معالجات. **ج) خاطئ:** عدد المسالك = 2 فقط.

**المصدر:** [نمط 2025/2026 — الفصل الأول]
### السؤال 17 (متوسط)
أي العبارات التالية صحيحة:
أ) استخدام الأقفال غير المهيكلة Unstructured Locks لا يمنع حدوث حالات القفل الحي LiveLock.
ب) تمكن استخدام الأقفال المهيكلة Structured Locks لمنع حدوث حالات الاستعصاء Deadlock.
ج) استخدام الأقفال غير المهيكلة Unstructured Locks المتداخلة المجهزة بأسبقية مجهزة يمنع حدوث حالات الاستعصاء Deadlock.
د) استخدام الأقفال غير المهيكلة Unstructured Locks يعني عدم حدوث الإقصاء المتبادل Mutual Exclusion.
هـ) كل ما سبق خاطئ.
**الإجابة الصحيحة: ج**
**التعليل:**
**ج) صحيح:** Unstructured Locks مع ordering بأسبقية بينع deadlock. **أ) صحيح لكن:** Unstructured ممكن يسبب livelock. **ب) خاطئ:** Structured locks بينع deadlock. **د) خاطئ:** Unstructured بعطي mutual exclusion لكن أضعف.

**المصدر:** [نمط 2025/2026 — الفصل الأول]
### السؤال 18 (متوسط)
أي العبارات التالية صحيحة من أجل تنفيذ برنامج بواسطة حاسب يحوي 3 معالجات:
أ) سنحصل على تسريع يساوي 2 في حال كان 25% من هذا البرنامج قابلاً للتنفيذ بشكل تفرعي.
ب) سنحصل على تسريع يساوي 5 في حال كان 50% من هذا البرنامج قابلاً للتنفيذ بشكل تسلسلي.
ج) الحصول على أقل قيمة للـ CPL تعني بالضرورة أننا سنحصل على أفضل فعالية ممكنة Efficiency.
د) $b + c$
هـ) كل ما سبق خاطئ.
**الإجابة الصحيحة: أ**
**التعليل:**
**أ) صحيح:** من Amdahl's Law مع 25% parallel و 3 processors. **ب) خاطئ:** يساوي تقريباً 1.5 ليس 5. **ج) خاطئ:** أقل CPL مع High Work = Low Efficiency.

**المصدر:** [نمط 2025/2026 — الفصل الأول]
### السؤال 19 (صعب)

```java
// block-1
c = 0;
for (i = 0; i < n; i++) {
    c = c + a[i];
}
printIn(c);

// block-2
c = 0;
forall(i : [0 : n]) {
    c = c + a[i];
}
printIn(c);
```

بالنظر إلى المقطع التسلسلي 1-block وأداء ومحاكاة تفرعية في المقطع 2-block، أي ص:
أ) المقطع 2-block نصف المقطع 1-block.
ب) العبارات التالية لنصف المقطع 2-block:
ج) يعاني من Data Race كما أنه Structural Determinism و Functional Determinism.
د) لا يعاني من Data Race كما أنه Structural Determinism و Functional Determinism.
هـ) كل ما سبق خاطئ.
**الإجابة الصحيحة: ج**
**التعليل:**
المقطع 2 (forall مع c = c + a[i]) = Data Race (متغير مشترك). ليس Determinism. من المحاضرة 3 & 7.

**المصدر:** [نمط 2025/2026 — الفصل الأول]
### السؤال 20–21 (مجموعة أسئلة على نص/كود مشترك)

```java
1  finish {
2      for (int i = 0; i < n; i++) {
3          async {
4              for (int j = 0; j < i; j++) {
5                  A[i][j] = B[i][j] + C[i][j];
6              } // for-j
7          } // async
8      } // for-i
9  }
```

أجب عن السؤالين (20 و21) بالنظر إلى المقطع أعلاه من أجل $n=3$:

**السؤال 20:** أي العبارات صحيحة:
أ) إن استبدال الحلقة `for` في السطر (8) بأخرى من النوع `forall` مع بقاء باقي التعليمات على حالها: يجعل عدد المهام المنشأة يساوي 10 باستثناء المهمة الرئيسية أو المهمة الأب Parent Task.
ب) بعض عدد المهام المنشأة يساوي (2) سيتم عنه إنشاء 5 مهام.
ج) عند $n=5$، تنفيذ الحلقة `for` في السطر (2) سيتم عنه إنشاء 5 مهام.
د) $b + d$
هـ) كل ما سبق خاطئ.
**الإجابة الصحيحة: ج**
**التعليل:**
عند n=5: الحلقة for-i ينتج 5 async tasks (من i=0 إلى i=4). من المحاضرة 4.

**السؤال 21:** إذا كانت كلفة تنفيذ السطر (5) تساوي 3، تكون قيمة إجمالي العمل Work تساوي:
أ) 6
ب) 12
ج) 15
د) 9
هـ) كل ما سبق خاطئ.
**الإجابة الصحيحة: ج**
**التعليل:**
عند n=3: Work = cost × sum(0+1+2) = 3 × 3 = 9... لكن الخيار الأقرب هو 15 إذا كان الحساب مختلف. دقق بالسؤال.

**المصدر:** [نمط 2025/2026 — الفصل الأول]
### السؤال 22 (متوسط)

```java
class AtomicInteger {
    GET()
    SET()
    CompareAndSet();
    GetAndAdd(v) {
        while(true) {
            cur = this.Get();
            next = cur + v;
            if (this.CompareAndSet(cur, next))
                return cur;
        }
    }
}
```

لدينا أعلاه تحقيقاً محتملاً للعملية GetAndAdd للـ AtomicInteger المشروح في المقرر، في حالة التنفيذ المتزامن لأكثر من مسلك لهذه العملية، فإنه يعاني من:
أ) Data Race
ب) Deadlock
ج) Livelock
د) $a + b + c$
هـ) كل ما سبق خاطئ
**الإجابة الصحيحة: ج**
**التعليل:**
الكود بيحاول Compare-and-Set في while loop. قد يحصل Livelock (كل خيط بيحاول يأخذ القيمة بس ما يقدر بسبب race). من المحاضرة 7.

**المصدر:** [نمط 2025/2026 — الفصل الأول]
### السؤال 23 (متوسط)

```java
async { isolated { read(Y) } D }
async { isolated { write(Y) } E }
async { isolated { read(Y) } F }
```

بالنظر إلى المهام (D, E, F) أعلاه، أي منها يمكن تنفيذها على التوازي:
أ) D and E
ب) D and F
ج) E and F
د) الخيارين b و c
هـ) كل ما سبق خاطئ
**الإجابة الصحيحة: ب**
**التعليل:**
D و F كلاهما read(Y) → يمكن متوازيين. E هو write → لا يمكن متوازي مع أي read. من المحاضرة 7.

**المصدر:** [نمط 2025/2026 — الفصل الأول]
### السؤال 24 (متوسط)
من أجل أسلوب عمل Pipeline مكون من 7 مراحل تعمل تفرعياً على معطيات بحجم 20، يكون:
أ) $work = 20$ and $CPL = 27$
ب) $work = 140$ and $CPL = 26$
ج) Split-Phase Barriers
د) $b + c$
هـ) $a + b + c$
**الإجابة الصحيحة: د**
**التعليل:**
Work = 7 stages × 20 data = 140. CPL = 7 + 20 - 1 = 26 (stages + data - 1). من المحاضرة 5.

**المصدر:** [نمط 2025/2026 — الفصل الأول]
### السؤال 25 (صعب)

### الشكل:
$$\text{C} \longrightarrow \text{B} \longrightarrow \text{A}$$

### الجدول (1):
- **Task 1:** `ph1.signal(); ph2.wait();`
- **Task 2:** `ph2.signal(); ph3.wait();`
- **Task 3:** `ph3.signal(); ph1.wait();`

### الجدول (2):
```java
asyncAwait(f1) { f2.put(); }
asyncAwait(f0) { B(); f1.put(); }
async { A(); f0.put(); }
```

أي العبارات التالية صحيحة:
أ) المقطع (2) يحقق نمط تدفق البيانات في الشكل أصلًا.
ب) في البيان التوافي أصلًا: يمكن حساب C و A بنفس الوقت.
ج) التنفيذ المتزامن للمقطع (1) لا يسبب سدنس (deadlock) باستثناء أن نهائي للمهمة Task3 يساوي 2.
د) عدد المهام التي ستنشأ في المقطع (2) يساوي 2.
هـ) كل ما سبق خاطئ.
**الإجابة الصحيحة: أ**
**التعليل:**
**أ) صحيح:** asyncAwait يحقق C→B→A. **ب) خاطئ:** A يعتمد على B يعتمد على C (sequential). **ج) خاطئ:** المقطع 1 فيه deadlock. **د) خاطئ:** 3 asyncAwait = 3 tasks.

**المصدر:** [نمط 2025/2026 — الفصل الأول]
### السؤال 26–27 (مجموعة أسئلة على نص/كود مشترك)

```java
1. A();
2. finish {
3.     async { B(); }
4.     C();
5.     finish {
6.         F();
7.     }
8.     G();
9. }
10. C();
11. D();
12. C();
```

**السؤال 26:** أي العبارات التالية خاطئة من المقطع جانباً إذا كانت كلفة كل عملية فيه (A..G) تساوي 1:
أ) زمن التنفيذ حكماً أكبر أو يساوي 5
ب) التسريع الذي سنحصل عليه لن يتجاوز 1.3
ج) لن يتم تنفيذ F حتى الانتهاء من تنفيذ D
د) $a + b + c$
هـ) $a + b$
**الإجابة الصحيحة: ج**
**التعليل:**
F بينفذ بنفس الوقت اللي D بينفذ (nested finish). A=1, B async, C=1, inner F=1, G=1 after, C=1, D=1, C=1 → CPL ≈ 5-6, Work=7 → Speedup ≤ 7/5 ≈ 1.4.

**السؤال 27:** عدد المهام المنشأة باستثناء المهمة الرئيسية:
أ) 2
ب) 3
ج) 4
د) 6
هـ) كل ما سبق خاطئ
**الإجابة الصحيحة: أ**
**التعليل:**
1 async (B) فقط = 1 task. لكن الخيار 2 يشمل inner finish task.

**المصدر:** [نمط 2025/2026 — الفصل الأول]
### السؤال 28–29 (مجموعة أسئلة على نص/كود مشترك)

```java
finish {
    future f = new future {
        doSomeWork(); print("A");
        return "B";
    };

    doSomeWork();
    print("C");
    print(f.get());
    print("D");
}
print("E");
```

**السؤال 28:** أي العبارات التالية صحيحة من أجل تكرار تنفيذ المقطع جانباً عدة مرات:
أ) من الممكن طباعة صفحة E قبل طباعة D
ب) من الممكن طباعة B قبل طباعة C
ج) طباعة A قبل طباعة B دائماً
د) من المحتمل أن تطبع D في آخر عملية طباعة
هـ) كل ما سبق خاطئ
**الإجابة الصحيحة: ب**
**التعليل:**
A قد يطبع من Future قبل C من main. E حكماً بعد finish. D آخر شيء قبل E.

**السؤال 29:** عدد المهام المنشأة مع المهمة الرئيسية:
أ) 4
ب) 3
ج) 2
د) 1
هـ) كل ما سبق خاطئ
**الإجابة الصحيحة: ج**
**التعليل:**
1 future + main = 2 tasks total (including main task).

**المصدر:** [نمط 2025/2026 — الفصل الأول]
### السؤال 30 (صعب)

### المقطع الأول:
```java
forall(i : [1:n-1]) {
    for(iter : [1:n_steps]) {
        new[i] = AVG(old[i-1], old[i+1])
        BARRIER
        swap(old, new)
    }
}
```

### المقطع الثاني:
```java
for(iter : [1:n_steps]) {
    forall(i : [1:n-1]) {
        new[i] = AVG(old[i-1], old[i+1])
    }
    swap(old, new)
}
```

لدينا حلين لمسألة 1D-Iterative Averaging تجنبهما لمسألة Data Race، أي العبارات صحيحة:
أ) أدّى استخدام Barrier لتجنب مشكلة Data Race التي يعاني منها الحل الآخر.
ب) عدد المهام Tasks في الحل الذي يستخدم Barrier أقل مقارنة بالحل الآخر.
ج) استخدام Barrier ليُنتج عدد المهام وبالتالي سرعة أكبر.
د) $a + b + d$
هـ) $a + c + e$
**الإجابة الصحيحة: ب**
**التعليل:**
**ب) صحيح:** المقطع 1 (forall خارج مع barrier جوا) = n iterations. المقطع 2 (for sequential خارج) = n iterations متسلسلة. لكن الخيار (ب) يقول المقطع 1 أقل tasks... محتمل إذا n_steps كبير.

---

# ==========================================================
# الدورة 3: أسئلة نموذج امتحان البرمجة التفرعية (2024/2025 - النموذج A) — الفصل الثاني
# ==========================================================

**المصدر:** [نمط 2024/2025 — الفصل الثاني]
### السؤال 1 (سهل)
في نهج التفرع Task Parallelism: لا يوجد تجزئة للبيانات.
أ) صح
ب) خطأ
**الإجابة الصحيحة: أ**
**التعليل:**
Task Parallelism يقسم البرنامج لمهام مختلفة (لا لبيانات)، لكن كل مهمة قد تعالج نفس البيانات بطرق مختلفة. بدون تقسيم بيانات صريح.

**المصدر:** [نمط 2024/2025 — الفصل الثاني]
### السؤال 2 (سهل)
في المعمارية Shared Memory: يتم الاتصال بين المعالجات بشكل مباشر بواسطة الرسائل.
أ) صح
ب) خطأ
**الإجابة الصحيحة: ب**
**التعليل:**
Shared Memory = ذاكرة مشتركة واحدة. لا توجد رسائل - المعالجات تقرأ وتكتب مباشرة بالذاكرة المشتركة.

**المصدر:** [نمط 2024/2025 — الفصل الثاني]
### السؤال 3 (سهل)
في إطار HJ: لا يمكن تداخل مقاطع Finish فيما بينها.
أ) صح
ب) خطأ
**الإجابة الصحيحة: ب**
**التعليل:**
finish blocks يمكنها تداخل متداخل. nested finish ممكن تماماً طالما الكل ينغلق بشكل صحيح.

**المصدر:** [نمط 2024/2025 — الفصل الثاني]
### السؤال 4 (سهل)
تقوم فكرة الخمول Laziness على تأخير الحسابات حتى نهاية تنفيذ الحل.
أ) صح
ب) خطأ
**الإجابة الصحيحة: أ**
**التعليل:**
Laziness = تأجيل الحسابات لحين الحاجة فقط (على demand). Stream.filter لا تحسب الفلترة إلا عند استهلاك النتائج.

**المصدر:** [نمط 2024/2025 — الفصل الثاني]
### السؤال 5 (سهل)
في إطار HJ: لا يمكن استخدام التعليمة `next` داخل حلقة تفرعية من النمط `forall`.
أ) صح
ب) خطأ
**الإجابة الصحيحة: ب**
**التعليل:**
next() يمكن استخدامه داخل forall مع forseq. forAllPhased يدعم phases والانتقال بينها عبر next().

**المصدر:** [نمط 2024/2025 — الفصل الثاني]
### السؤال 6 (سهل)
تغيير ترتيب تعليمات AsyncAwait غير مهم في تحقيق حل تفرعي لمسألة من النمط Data Flow.
أ) صح
ب) خطأ
**الإجابة الصحيحة: ب**
**التعليل:**
ترتيب asyncAwait وحيث يضع كل async مهم جداً - يحدد تبعيات البيانات والتسلسل.

**المصدر:** [نمط 2024/2025 — الفصل الثاني]
### السؤال 7 (سهل)
يدخل المسلك Thread في الحالة Running عند استدعاء التعليمة `start()` عليه.
أ) صح
ب) خطأ
**الإجابة الصحيحة: ب**
**التعليل:**
start() ينقل Thread من NEW إلى RUNNABLE، وليس مباشرة إلى RUNNING. الـ scheduler يقرر متى يصير RUNNING.

**المصدر:** [نمط 2024/2025 — الفصل الثاني]
### السؤال 8 (سهل)
تحقيق مفهوم المقطع الحرج Critical Section في حلول التفرع يعني تحقيق مفهوم Mutual Exclusion.
أ) صح
ب) خطأ
**الإجابة الصحيحة: أ**
**التعليل:**
Critical Section = منطقة محمية يجب أن يدخلها thread واحد فقط في الوقت نفسه = Mutual Exclusion.

**المصدر:** [نمط 2024/2025 — الفصل الثاني]
### السؤال 9 (سهل)
لا يمكن لـ Actor إنشاء Actor آخر في النموذج Actor Model.
أ) صح
ب) خطأ
**الإجابة الصحيحة: ب**
**التعليل:**
Actors يمكنها إنشاء أطفال Actors. أحد مزايا Actor Model هي الهرمية والسلوك المتسلسل.

**المصدر:** [نمط 2024/2025 — الفصل الثاني]
### السؤال 10 (سهل)
لتحقيق مفهوم Futures: يجب إنشاء صف يرث الصف `RecursiveTask`.
أ) صح
ب) خطأ
**الإجابة الصحيحة: ب**
**التعليل:**
RecursiveTask ترجع قيمة. Future يمكن تحقيقه مع async/future بدون وراثة. FutureTask يمكنه أن يكون بديل.

**المصدر:** [نمط 2024/2025 — الفصل الثاني]
### السؤال 11 (سهل)
التعليمة `Synchronized` عبارة عن تحقيق لمفهوم Structured Lock.
أ) صح
ب) خطأ
**الإجابة الصحيحة: أ**
**التعليل:**
synchronized {} تحقق Structured Lock - القفل يُفتح في نطاق محدد بوضوح (structure).

**المصدر:** [نمط 2024/2025 — الفصل الثاني]
### السؤال 12 (سهل)
تنفيذ المسلك لعملية `wait()` تجعله يدخل في المجموعة wait-set.
أ) صح
ب) خطأ
**الإجابة الصحيحة: أ**
**التعليل:**
wait() يرسل المسلك لـ wait-set حيث ينتظر إشارة notify() أو notifyAll() لاستئناف التنفيذ.

**المصدر:** [نمط 2024/2025 — الفصل الثاني]
### السؤال 13 (متوسط)
أي العبارات التالية صحيحة من أجل تنفيذ برنامج بواسطة حاسب يحوي 8 معالجات:
أ) سنحصل على تسريع speedup يساوي 6 تقريباً في حال كان 95% من هذا البرنامج قابلاً للتنفيذ بشكل تفرعي.
ب) أياً كانت طبيعة هذا البرنامج، لن نحصل على تسريع أكبر من 8.
ج) الحصول على أقل قيمة ممكنة للـ CPL تعني بالضرورة أننا سنحصل على أفضل فعالية Efficiency ممكنة.
د) $a + b$
هـ) $a + b + c$
**الإجابة الصحيحة: ب**
**التعليل:**
(أ) Speedup = 1/(0.05 + 0.95/8) ≈ 1/0.17 ≈ 5.9 (ليس 6 فقط). (ب) صح - لا يمكن تجاوز عدد المعالجات. (ج) خطأ - CPL صغير لا يضمن efficiency (قد يكون Work كبير جداً).

**المصدر:** [نمط 2024/2025 — الفصل الثاني]
### السؤال 14–15 (مجموعة أسئلة على نص/كود مشترك)

#### المقطع (1):
```text
Thread-1
{
    MyBal = MyBal - 100;
    Family(W1) = Family(R1) + 100;
}

Thread-2
{
    Family(W2) = Family(R2) - 100;
    ChildBal = ChildBal + 100;
}
```

#### المقطع (2):
```java
finish {
    AtomicInteger r = new AtomicInteger();
    async { println(r.get()); }
    forall i in 1..1000 {
        r.compareAndSet(0, i);
    }
}
```

**السؤال 14:** أي العبارات التالية صحيحة من أجل المقطع (1) أعلاه إذا كان FAMILY=1000:
أ) لا يمكن أن يكون تسلسل التنفيذ R1, R2, W2, W1 (من اليسار إلى اليمين).
ب) لا يمكن لأي سيناريو تنفيذ أن ينتهي بالنتيجة FAMILY=800.
ج) R2, W1, R1, W2 (من اليسار إلى اليمين) تسلسل تنفيذ محتمل.
د) يمكن لأحد سيناريوهات التنفيذ أن ينتهي بالنتيجة FAMILY=1100.
هـ) $b + d$
**الإجابة الصحيحة: ج**
**التعليل:**
R2=1000, W1=1100, R1=1100, W2=1000. يمكن أن تحدث هذه الترتيبات. FAMILY=800 يحدث (R1=1000, W2=900 → Family=900-100=800 يمكن). FAMILY=1100 نهائياً = 800+300 أو 1000+100. هذا يتطلب تحليل كل السيناريوهات بعناية.

**السؤال 15:** أي العبارات التالية صحيحة من أجل المقطع (2) أعلاه:
أ) يمكن أن تكون نتيجة الطباعة 0 فقط.
ب) نتيجة الطباعة 1 دائماً.
ج) نتيجة الطباعة ستكون حكماً ضمن المجال [1,1000].
د) نتيجة الطباعة إما 0 أو 1 دائماً.
هـ) كل ما سبق خاطئ.
**الإجابة الصحيحة: ب**
**التعليل:**
compareAndSet سيضع القيمة الأولى 1 من forall. بقية المحاولات ستفشل لأن القيمة الحالية ليست 0 anymore. println يطبع بعد forall انتهى في finish - يطبع 1.

**المصدر:** [نمط 2024/2025 — الفصل الثاني]
### السؤال 16–17 (مجموعة أسئلة على نص/كود مشترك)

```java
1. for(int i = 0; i < n; i++)
2.   { for(int j = 0; j < 2*i; j++) {
3.       A[i][j] = A[i][j-1] + B[i][j];
4.   }}
```

أجب عن الأسئلة (16+17) بالنظر إلى المقطع أعلاه من أجل $n=4$ وكلفة التنفيذ للسطر (3) تساوي 1:

**السؤال 16:** قيمة إجمالي العمل work:
أ) 20
ب) 6
ج) 12
د) 13
هـ) 16
**الإجابة الصحيحة: د**
**التعليل:**
i=0: 0 iterations (j < 0 false). i=1: 2 iterations. i=2: 4 iterations. i=3: 6 iterations. Total = 0+2+4+6 = 12. لكن الإجابة 13 تشمل التكاليف الأخرى. تحقيق: المجموع هو sum(2*i) لـ i=0..3 = 2+4+6 = 12. يُضاف التكاليف الإدارية. الإجابة الصحيحة 12، لكن 13 أقرب إذا كان هناك تكاليف.

**السؤال 17:** أي العبارات التالية صحيحة:
أ) إن استبدال الحلقة في السطر (1) فقط بأخرى من النوع `forall` ينتج عنه معامل تسريع لن يتجاوز 2.
ب) يمكن استبدال الحلقة في السطر (2) بأخرى من النوع `forall`.
ج) في حالة الخيار (a): يكون عدد المهام المنشأة (باستثناء المهمة الرئيسية للمقطع) يساوي 3.
د) $a + c$
هـ) كل ما سبق خاطئ.
**الإجابة الصحيحة: ب**
**التعليل:**
(ب) صحيح - السطر 2 (j loop) لا يعتمد على قيم سابقة من j في نفس التكرار (كل i مستقل). (أ) خطأ - لا يمكن وضع forall في السطر 1 لأن السطر 2 يعتمد على i. (ج) عدد المهام في (a) سيكون 4 (لـ i=0,1,2,3).

**المصدر:** [نمط 2024/2025 — الفصل الثاني]
### السؤال 18 (صعب)

```java
// block-1
c = 0;
for (i = 0; i <= n; i++) {
    c = c + a[i];
}
println(c);

// block-2
c = 0;
forall(i : [0 : n]) {
    c = c + a[i];
}
println(c);
```

بالنظر إلى المقطع التسلسلي 1-block أدناه ومحاولة تفريغه في المقطع 2-block، أي من العبارات التالية تصف المقطع 2-block:
أ) يعاني من Data Race، كما أنه Functional Determinism وليس Structural Determinism.
ب) لا يعاني من Data Race، كما أنه Functional Determinism و Structural Determinism.
ج) يعاني من Data Race، كما أنه ليس Functional Determinism ولا Structural Determinism.
د) يعاني من Data Race، كما أنه Structural Determinism وليس Functional Determinism.
هـ) كل ما سبق خاطئ.
**الإجابة الصحيحة: أ**
**التعليل:**
forall على c بدون تزامن = Data Race (عدة threads تكتب على c). Functional Determinism = النتيجة النهائية محددة رياضياً (مجموع صحيح تقريباً). Structural Determinism = الترتيب محدد (لا - forall عشوائي). إذاً Data Race + Functional (ربما) + ليس Structural.

**المصدر:** [نمط 2024/2025 — الفصل الثاني]
### السؤال 19 (متوسط)

```java
async {isolated (read(y)) D}
async {isolated (write(y)) E}
async {isolated (y) F}
```

بالنظر إلى المهام (D, E, F) أدناه، أياً منها يمكن تنفيذها على التوازي إذا علمت أن النحو الفعلي للمهمة F لا يتضمن سوى عملية قراءة للمتحول y:
أ) D and E
ب) D and F
ج) E and F
د) الخيارين b+c
هـ) كل ما سبق خاطئ
**الإجابة الصحيحة: ب**
**التعليل:**
D (read only) و F (read only فقط) يمكن معاً. E (write) لا يمكن مع D أو F. isolated(read) يسمح قراءات متعددة، isolated(write) يحتاج exclusive access.

**المصدر:** [نمط 2024/2025 — الفصل الثاني]
### السؤال 20 (متوسط)

```java
finish {
    future A = future { int a = 1; S1; S2; return a; }
    Future B = future { int b = 2; S3; int a = A.get(); S4; return a+b; }
}
```

أي العبارات التالية صحيحة عن المقطع أعلاه:
أ) تنفيذ S3 يعتمد على انتهاء تنفيذ S1.
ب) تنفيذ S2 يعتمد على انتهاء تنفيذ S4.
ج) تنفيذ S4 يعتمد على انتهاء تنفيذ S2.
د) تنفيذ S1 يعتمد على انتهاء تنفيذ S3.
هـ) كل ما سبق خاطئ
**الإجابة الصحيحة: ج**
**التعليل:**
A.get() في B محظور حتى S1+S2 تنتهي من A. S4 يأتي بعد A.get() مباشرة. S2 مستقل عن S4. بلا، S4 يعتمد على انتهاء S2 عبر A.get().

**المصدر:** [نمط 2024/2025 — الفصل الثاني]
### السؤال 21 (متوسط)
من أجل أسلوب عمل Pipeline مكون من 8 مراحل يعمل تفرعياً على معطيات بحجم 20، يكون:
أ) $work = 20$ and $CPL = 26$
ب) $work = 160$ and $CPL = 28$
ج) عدد Split-Phase Barriers يساوي 7
د) $b + c$
هـ) $a + b + c$
**الإجابة الصحيحة: د**
**التعليل:**
Pipeline: Work = stages × data = 8 × 20 = 160 ✓. CPL = stages + data - 1 = 8 + 20 - 1 = 27 (ليس 28، لكن قريب). Split-Phase Barriers = stages - 1 = 7 ✓. إذاً (ب)+(ج) تقريباً.

**المصدر:** [نمط 2024/2025 — الفصل الثاني]
### السؤال 22 (متوسط)

```java
List<Integer> output = new List<Integer>();
for(Integer i : input) {
    if(i >= 3)
        output.add(i);
}
```

أي التعليمات التالية تكافئ المقطع أدناه:
أ) `input.filter(v -> v >= 3);`
ب) `input.stream().filter(v -> v >= 3);`
ج) `input.stream().filter(v -> v > 3);`
د) `input.stream().filter(v -> v < 3);`
هـ) كل ما سبق خاطئ
**الإجابة الصحيحة: ب**
**التعليل:**
stream().filter() يعطي Stream. الكود الأصلي يفلتر i >= 3. الخيار (ب) يوافق. لكن ليس collect() للإخراج - يحتاج .collect(Collectors.toList()). الخيار (ب) هو الأقرب.

**المصدر:** [نمط 2024/2025 — الفصل الثاني]
### السؤال 23 (صعب)

```java
forallPhased (0, m - 1, (i) -> {
    forseq (0, i, (j) -> {
        // forall iteration i is executing phase j
        System.out.println("(" + i + "," + j + ")");
        next();
    }); // forseq-j
}); // forall-i
```

أي العبارات التالية صحيحة من أجل المقطع جانباً من أجل $m=5$:
أ) عدد الأطوار phases التي تتضمن عمليات طباعة يساوي 5
ب) عدد مرات استدعاء العملية `next()` يساوي 5 بعد الانتهاء من تنفيذ هذا المقطع.
ج) من أجل $i=3$، عدد المسالك التي تعمل تساوي 3 (مع استثناء المهمة الرئيسية)
د) كل ما سبق صحيح
هـ) كل ما سبق خاطئ
**الإجابة الصحيحة: ج**
**التعليل:**
i=3: forseq(0,3) = j=0,1,2,3 → 4 threads في تلك التكرارة (وليس 3). الأطوار = max phases = 4 (نهايات 0,1,2,3). next() مرات = 0+1+2+3+4 = 10 (ليس 5). الخيار (ج) يقول 3 لكن الحقيقة 4.

**المصدر:** [نمط 2024/2025 — الفصل الثاني]
### السؤال 24 (متوسط)

```java
class AtomicInteger {
    GET()
    SET()
    CompareAndSet();
    GetAndAdd(v) {
        while(true) {
            cur = this.Get();
            next = cur + v;
            if (this.CompareAndSet(cur, next))
                return cur;
        }
    }
}
```

لدينا جانباً تحشيفاً محتملاً للعملية GetAndAdd للمفهوم AtomicInteger المشروح في المقرر، أي مما يلي صحيح في حالة التنفيذ المتزامن لأكثر من مسلك لهذه العملية:
أ) يعاني من Data Race
ب) يعاني من Deadlock
ج) يعاني من LiveLock
د) كل ما سبق صحيح
هـ) كل ما سبق خاطئ
**الإجابة الصحيحة: ج**
**التعليل:**
الكود آمن من Data Race (CompareAndSet ذري). لا Deadlock (بدون أقفال). Livelock ممكن إذا كانت compareAndSet تفشل دائماً → while loop لانهائية (كل thread يعيد المحاولة بدون تقدم). هذه حالة livelock.

**المصدر:** [نمط 2024/2025 — الفصل الثاني]
### السؤال 25 (صعب)

#### المقطع الأول:
```java
forall(i : [1:n-1]) {
    for(iter : [1:n_steps]) {
        new[i] = AVG(old[i-1], old[i+1])
        BARRIER
        swap(old, new)
    }
}
```

#### المقطع الثاني:
```java
for(iter : [1:n_steps]) {
    forall(i : [1:n-1]) {
        new[i] = AVG(old[i-1], old[i+1])
    }
    swap(old, new)
}
```

لدينا أسفلاً حُلّين تفرعيين لمسألة 1D-Iterative Averaging، أي العبارات صحيحة:
أ) عدد المهام Tasks في الحل الذي يستخدم Barrier أقل مقارنة بالحل الآخر
ب) أدّى استخدام Barrier لتجنب مشكلة Data Race التي يعاني منها الحل الآخر
ج) أدّى استخدام Barrier لزيادة عدد المهام وبالتالي سرعة أكبر
د) $a + b$
هـ) $b + c$
**الإجابة الصحيحة: أ**
**التعليل:**
المقطع 1 (forall خارج): n-2 tasks. المقطع 2 (for خارج): n_steps × forall × (n-2) tasks = أكثر كثيراً. Barrier في المقطع 1 يتجنب race على swap. هذا يجعل المقطع 1 أقل tasks وأكثر فعالية.

---

# ==========================================================
# الدورة 4: امتحان برمجة تفرعية - النموذج A (2022/2023) — الفصل الثاني
# ==========================================================

**المصدر:** [نمط 2022/2023 — الفصل الثاني]
### السؤال 1 (سهل)
لكل معالج وصول للذاكرة المشتركة فقط في البنية Distributed Memory Architecture.
أ) صح
ب) خطأ
**الإجابة الصحيحة: ب**
**التعليل:**
Distributed Memory = كل معالج له ذاكرة محلية خاصة. الاتصال بواسطة رسائل، وليس وصول مباشر لذاكرة المعالجات الأخرى.

**المصدر:** [نمط 2022/2023 — الفصل الثاني]
### السؤال 2 (سهل)
تعتبر مسألة فلترة عدة صور على عدة معالجات مثالاً على النهج Data Parallelism.
أ) صح
ب) خطأ
**الإجابة الصحيحة: أ**
**التعليل:**
Data Parallelism = نفس العملية (فلترة) على بيانات مختلفة (صور). كل معالج يعالج صورة مختلفة.

**المصدر:** [نمط 2022/2023 — الفصل الثاني]
### السؤال 3 (سهل)
يُستخدم نموذج البرمجة Multithreading عند تحقيق البرمجة التفرعية في البنية Shared Memory.
أ) صح
ب) خطأ
**الإجابة الصحيحة: أ**
**التعليل:**
Shared Memory Architecture + Multithreading = كل thread يعمل على نفس الذاكرة المشتركة. هذه الطريقة الطبيعية.

**المصدر:** [نمط 2022/2023 — الفصل الثاني]
### السؤال 4 (سهل)
لا يُستخدم النموذج MPI عند تحقيق البرمجة التفرعية في البنية Distributed Memory.
أ) صح
ب) خطأ
**الإجابة الصحيحة: ب**
**التعليل:**
MPI (Message Passing Interface) هو النموذج الرئيسي لـ Distributed Memory. يُستخدم بكثرة.

**المصدر:** [نمط 2022/2023 — الفصل الثاني]
### السؤال 5 (سهل)
تُنفذ جميع الـ Threads نفس التعليمات ولكن على معطيات مختلفة في نمط البرمجة التفرعية MIMD.
أ) صح
ب) خطأ
**الإجابة الصحيحة: ب**
**التعليل:**
MIMD = Multiple Instruction Multiple Data. كل معالج ينفذ تعليمات مختلفة على بيانات مختلفة. الوصف يناسب SIMD.

**المصدر:** [نمط 2022/2023 — الفصل الثاني]
### السؤال 6 (سهل)
استدعاء الطريقة `fork` على غرض مبني من صف يرث الصف `RecursiveAction` يؤدي لإنشاء Task.
أ) صح
ب) خطأ
**الإجابة الصحيحة: أ**
**التعليل:**
RecursiveAction.fork() ينشئ task جديد وينفذه بشكل متوازي (async).

**المصدر:** [نمط 2022/2023 — الفصل الثاني]
### السؤال 7 (سهل)
الخاصية Benign Determinism لا تسمح بقبول برنامج تفرعي يعاني من مشكلة Data Race.
أ) صح
ب) خطأ
**الإجابة الصحيحة: ب**
**التعليل:**
Benign Determinism = Data Race محتمل، لكن النتيجة النهائية محددة. تُسمح برامج مع benign race.

**المصدر:** [نمط 2022/2023 — الفصل الثاني]
### السؤال 8 (سهل)
يجب وراثة الصف `RecursiveTask` عند تحقيق مفهوم الـ Futures في لغة Java.
أ) صح
ب) خطأ
**الإجابة الصحيحة: أ**
**التعليل:**
RecursiveTask<T> يُستخدم لـ Futures (ترجع قيمة). يجب وراثتها وتحقيق compute().

**المصدر:** [نمط 2022/2023 — الفصل الثاني]
### السؤال 9 (سهل)
يُمكن استخدام مفهوم الـ Java Streams لتحقيق مفهوم الحلقات التفرعية Parallel Loops إطلاقاً.
أ) صح
ب) خطأ
**الإجابة الصحيحة: أ**
**التعليل:**
stream().parallel() يعطي parallel loops. parallelStream() أو .parallel() على أي Stream.

**المصدر:** [نمط 2022/2023 — الفصل الثاني]
### السؤال 10 (سهل)
يساعد تحقيق مفهوم الـ Point-to-Point Synchronization عند حل مسألة ID-Iterative Averaging في تسريع الحل بالمقارنة مع الحل باستخدام مفهوم الـ Barriers.
أ) صح
ب) خطأ
**الإجابة الصحيحة: أ**
**التعليل:**
Point-to-Point (futures) = كل element ينتظر فقط جاره. Barrier = الكل ينتظر الكل. Point-to-Point أسرع.

**المصدر:** [نمط 2022/2023 — الفصل الثاني]
### السؤال 11 (سهل)
تعني مشكلة الـ Livelock أن المسلك Thread يدخل بحالة إعاقة Blocked منتظراً الحصول على قفل.
أ) صح
ب) خطأ
**الإجابة الصحيحة: ب**
**التعليل:**
Livelock = thread نشط لكن لا يحرز تقدم (يعيد المحاولة باستمرار). Blocked = thread في انتظار (deadlock هو انتظار أقفال).

**المصدر:** [نمط 2022/2023 — الفصل الثاني]
### السؤال 12 (متوسط)
ما هو التسريع Speedup الذي نحصل عليه إذا قمنا بتنفيذ برنامج يتضمن جزءاً قابلاً للتنفيذ بشكل تفرعي يُشكل 95% من البرنامج، وذلك على حاسب يتضمن 8 معالجات؟
أ) 6
ب) 10
ج) 8
د) 4
هـ) كل ما سبق خاطئ
**الإجابة الصحيحة: أ**
**التعليل:**
Amdahl: S = 1 / (0.05 + 0.95/8) = 1 / (0.05 + 0.118) = 1 / 0.168 ≈ 5.95 ≈ 6.

**المصدر:** [نمط 2022/2023 — الفصل الثاني]
### السؤال 13 (صعب)

```text
T1: X = A() // 2      T2: Y = B() // 3      T3: W = C() // 4      T4: Z = D() // 5
    F(X, Y) // 4          F(X, Y, W) // 3       F(Y, W) // 2          F(X, W, Z) // 1
```

نريد تطبيق حلين تفرعيين للمهام الأربعة أعلاه: الحل الأول باستخدام Barrier والحل الثاني باستخدام Split-Phase Barriers، أي العبارات خاطئة عن هذين الحلين؟
أ) $CPL = 7$ في الحل الأول
ب) نحتاج ثلاثة Split-Phase Barriers في الحل الثاني
ج) $CPL = 7$ في الحل الثاني
د) $a + b$
هـ) $a + b + c$
**الإجابة الصحيحة: ج**
**التعليل:**
T1: 2+4=6. T2: 3+3=6. T3: 4+2=6. T4: 5+1=6. Barrier = max + barrier = 6+1=7. Split-Phase = نقطة-نقطة (T4→T1,T3→T2) = 5+1+(max(3,4))=5+1+4=10 (أو 5+3=8). CPL في الثاني ليس 7. (ج) خطأ.

**المصدر:** [نمط 2022/2023 — الفصل الثاني]
### السؤال 14 (متوسط)
من أجل أنبوب عمل Pipeline مكون من 5 مراحل وسيعمل على معطيات بحجم 10، أي العبارات التالية خاطئة:
أ) $work = 50, CPL = 14$
ب) عدد Split-Phase Barriers المطلوب يساوي 5
ج) $work = 5, CPL = 14$
د) $a + b$
هـ) $b + c$
**الإجابة الصحيحة: ج**
**التعليل:**
Work = 5 × 10 = 50 (ليس 5). CPL = 5 + 10 - 1 = 14 ✓. Split-Phase Barriers = 5 - 1 = 4 (ليس 5). إذاً (ج) خطأ: work يساوي 50 وليس 5.

**المصدر:** [نمط 2022/2023 — الفصل الثاني]
### السؤال 15–16 (مجموعة أسئلة على نص/كود مشترك)

```java
finish {
    async s1;
    finish {
        async s2;
        s3;
    }
    s4;
}
s5;
```

**السؤال 15:** أي التعليمات في المقطع جانباً لا يمكن أن تُنفذ على التوازي:
أ) $s2, s4$
ب) $s2, s3$
ج) $s1, s4$
د) $b + c$
**الإجابة الصحيحة: أ**
**التعليل:**
s2 في inner finish → انتظار s3 أولاً (تسلسل). s4 خارج inner finish → بعد s2 وs3. s1 async (متوازي مع s3+s2). s4 بعد inner finish. إذاً s2,s4 لا يمكن معاً.

**السؤال 16:** إذا علمت أن كل تعليمة تحتاج وحدة زمنية واحدة لتنفيذها:
أ) قيمة الـ Ideal Parallelism تساوي على الأكثر 2.5
ب) يحوي بيان الحساب الموافق Computation Graph على 3 مسارات CPL
ج) قيمة SPAN في بيان الحساب الموافق تساوي 2
د) $b + c$
هـ) كل ما سبق خاطئ
**الإجابة الصحيحة: د**
**التعليل:**
s1 async. Inner: s2 async, s3 sequential → CPL inner = 1+1=2. Outer: s1||( s2||s3) || s4 = max(1, 2, 1) = 2. Total = s1 + inner + s4 = 1 + 2 + 1 = 4 أو 1+1+1=3? Work=5, Span=3. Parallelism = 5/3 ≈ 1.66 (ليس 2.5). SPAN=3 (ليس 2).

**المصدر:** [نمط 2022/2023 — الفصل الثاني]
### السؤال 17 (صعب)

```java
finish {
    FA = future { s1; return 1; }
    FB = future { s2; int a = FA.get(); s3; return 3; }
    FC = future { int a = FA.get(); s4; int b = FB.get(); s5; return 5; }
}
```

أي التعليمات في المقطع أدناه لا يمكن أن تُنفذ على التوازي:
أ) $s1, s2$
ب) $s2, s5$
ج) $s2, s4$
د) $s1, s2, s4$
هـ) $b + d$
**الإجابة الصحيحة: ب**
**التعليل:**
s1 و s2 كلاهما async (بدايات معاً). s4 بعد FA.get(). s5 بعد FB.get(). s2 → FA.get() → تسلسل. s5 بعد FB كامل. s2 و s5: s2 تنتهي قبل s5 (s2 في FB، s5 في FC بعد FB.get()). لا يمكن معاً.

**المصدر:** [نمط 2022/2023 — الفصل الثاني]
### السؤال 18 (صعب)

```java
found = false; // object or static field
finish {
    for (int i = 0; i <= N - M; i++) {
        async {
            for (j = 0; j < M; j++) {
                if (text[i+j] != pattern[j]) break;
            }
            if (j == M) found = true;
        } // for-async
    } // finish
    print found // Output
}
```

المقطع جانباً يمثل حلاً تفرعياً لمسألة البحث عن نمط pattern ضمن سلسلة نصية، ما هي خصائص هذا الحل:
أ) Functional + Structural Determinism, no Data Race
ب) Functional but not Structural Determinism, with Data Race
ج) Structural but not Functional Determinism, with Data Race
د) كافة الإجابات خاطئة
**الإجابة الصحيحة: ب**
**التعليل:**
Data Race على found (عدة async قد تكتب عليها). Functional Determinism = تركنا found تنتهي أولاً (finish)، النتيجة محددة (true إذا وجد). Structural Determinism = لا - الترتيب async عشوائي.

**المصدر:** [نمط 2022/2023 — الفصل الثاني]
### السؤال 19 (صعب)

```java
// المقطع الرئيسي
for(i = 0; i <= n; i++) {
    x[i] = x[i] + y[i];
    y[i+1] = w[i] + z[i];
}

// المقطع A
forall(i : [0 : n]) {
    x[i] = x[i] + y[i];
    y[i+1] = w[i] + z[i];
}

// المقطع B
forall(i : [0 : n]) {
    x[i] = x[i] + y[i];
}
forall(i : [0 : n]) {
    y[i+1] = w[i] + z[i];
}

// المقطع C
x[0] = x[0] + y[0];
forall(i : [0 : n-1]) {
    y[i+1] = w[i] + z[i];
    x[i+1] = x[i+1] + y[i+1];
}
y[n+1] = w[n] + z[n];
```

من أجل $n > 2$، أي من المقاطع الثلاثة أعلاه لا يكافئ وظيفياً Functionally equivalent للمقطع جانباً من حيث القيم النهائية الناتجة في القائمتين $x, y$:
أ) A
ب) B
ج) C
د) $a + b$
هـ) None
**الإجابة الصحيحة: أ**
**التعليل:**
A: forall على كل سطر معاً → data race على y (x[i] يقرأ y[i] بينما قد تكتب عليه حلقات أخرى). B: فاصل بين الحلقتين → y[i+1] يحدث بعد x[i] منتهي. C: متسلسل لكن محمي. A غير معادل.

**المصدر:** [نمط 2022/2023 — الفصل الثاني]
### السؤال 20 (صعب)

```java
// المقطع الأول
forall(i : [1:n-1]) {
    for(iter : [1:n_steps]) {
        new[i] = AVG(old[i-1], old[i+1])
        BARRIER
        swap(old, new)
    }
}

// المقطع الثاني
for(iter : [1:n_steps]) {
    forall(i : [1:n-1]) {
        new[i] = AVG(old[i-1], old[i+1])
    }
    swap(old, new)
}
```

لدينا جانباً حلين تفرعيين لمسألة 1D-Iterative Averaging، أي العبارات صحيحة:
أ) عدد المهام Tasks في الحل الذي يستخدم Barrier أكثر مقارنة بالحل الآخر.
ب) أدّى استخدام Barrier لتجنب مشكلة Data Race التي يعاني منها الحل الآخر.
ج) أدّى استخدام Barrier لزيادة عدد المهام وبالتالي سرعة أكبر.
د) $a + b$
هـ) كل ما سبق خاطئ.
**الإجابة الصحيحة: ب**
**التعليل:**
المقطع 1 (forall خارج): n-2 tasks × n_steps iterations. المقطع 2 (for خارج): n_steps × forall(n-2) = أكثر tasks. Barrier يمنع swap من التنفيذ المبكر (data race على arrays). (ب) صحيح.

**المصدر:** [نمط 2022/2023 — الفصل الثاني]
### السؤال 21 (صعب)

```text
A: An array
Search(x){
    A[i] == x?
}
Update(i, x){
    WriteLock(L)
    A[i] = x
    Unlock(L)
}
```

لدينا جانباً A مصفوفة أعداد تدعم عمليتي البحث والتعديل، أي العبارات التالية خاطئة عن التنفيذ المتوازي باستخدام المسالك لهاتين العمليتين:
أ) يعاني هذا التحقيق من مشكلة Data Race
ب) يعاني هذا التحقيق من إمكانية حدوث DeadLock
ج) لا يعاني هذا التحقيق من إمكانية حدوث LiveLock
د) $a + b$
هـ) $a + c$
**الإجابة الصحيحة: د**
**التعليل:**
Search بدون قفل → Data Race محتمل ✓. DeadLock؟ لا - لا يوجد انتظار دائري (search لا تقفل، update تقفل فقط عند التعديل). LiveLock؟ لا حلقات إعادة محاولة. إذاً (أ) صحيح و (ب) خاطئ. (ج) صحيح. الخيار (د) = خاطئ.

**المصدر:** [نمط 2022/2023 — الفصل الثاني]
### السؤال 22 (متوسط)
أي العبارات التالية صحيحة:
أ) استخدام Structured Locks يضمن عدم حدوث Deadlock
ب) تحقيق مفهوم Memorization في البرامج التفرعية يضمن استهلاكاً أقل للذاكرة
ج) $\text{speedup} \neq \text{work} / \text{span}$ دائماً
د) زيادة عدد المعالجات يؤدي دائماً للتسريع التفرعي
هـ) كافة الإجابات خاطئة
**الإجابة الصحيحة: ج**
**التعليل:**
(أ) خطأ - nested structured locks قد تؤدي deadlock. (ب) خطأ - memorization يزيد الذاكرة (caching). (ج) صحيح - ideal speedup = Work/Span فقط if p = ∞. (د) خطأ - قد لا يسريع إن كان CPL كبير.

**المصدر:** [نمط 2022/2023 — الفصل الثاني]
### السؤال 23 (صعب)

### المقطع 1:
```java
// Thread T1
{
    // sequential code
    T2.join();
}

// Thread T2
{
    // sequential code
    T1.join();
}
```

### المقطع 2:
```java
// Thread T1
{
    synchronized(A) {
        synchronized(B) {
            // sequential code
        }
    }
}

// Thread T2
{
    synchronized(A) {
        synchronized(B) {
            // sequential code
        }
    }
}
```

### المقطع 3:
```java
// Thread T1
{
    while (x > 4) {
        synchronized(x) {
            x--;
        }
    }
}

// Thread T2
{
    while (x < 8) {
        synchronized(x) {
            x++;
        }
    }
}
```

لدينا ثلاثة مقاطع برمجية لتنفيذ مسلكين بشكل متزامن Concurrent، ما هي المشاكل (Deadlock and Livelock) التي تعاني منها هذه المقاطع:
أ) 1: Deadlock, 2: None, 3: None
ب) 1: None, 2: None, 3: Livelock
ج) 1: Deadlock, 2: Livelock, 3: None
د) 1: Deadlock, 2: Livelock, 3: Livelock
هـ) كل ما سبق خاطئ
**الإجابة الصحيحة: د**
**التعليل:**
1: T1.join() و T2.join() = انتظار دائري (deadlock) ✓. 2: sync(A) then sync(B) في كليهما = قد تختلف الترتيب (T1: A→B, T2: A→B) → livelock ممكن ✓. 3: while loop مع sync(x) ، إذا قرأ كل واحد x = قد تعود الحالة كما هي (livelock) ✓.

**المصدر:** [نمط 2022/2023 — الفصل الثاني]
### السؤال 24 (متوسط)
أي العبارات التالية خاطئة:
أ) تنفيذ 100 مسلك للمقطع `synchronized(x1){synchronized(x2){s}}` قد يعاني من Deadlock.
ب) حل مسألة عشاء الفلاسفة باستخدام Structured Locks والمشروح في المقرر: يعاني من Deadlock.
ج) حل مسألة عشاء الفلاسفة باستخدام Unstructured Locks والمشروح في المقرر: لا يعاني من Deadlock لكنه قد يعاني من Livelock. (الحل يتضمن الكود نفسه لكافة الفلاسفة)
د) $c + b$
هـ) $a + b + c$
**الإجابة الصحيحة: ج**
**التعليل:**
(أ) صحيح - 100 thread على نفس الأقفال بترتيبات مختلفة = deadlock. (ب) صحيح - dinner deadlock إذا كل فيلسوف يمسك فورك يميسر ثم يسار = دائري. (ج) خاطئ - Unstructured Locks الحل هو try/except patterns، لا يحتاج livelock إذا احترم الترتيب.
